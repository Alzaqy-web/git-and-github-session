{
  // explicit type / explicit annotation
  let price: number = 20000;
  let discount: number = 0.2;

  // implicit type
  // data type tidak diberin
  let message = "HELLO THERE";

  // message = null;

  // discount = 0.5;
  // discount = "0.9";

  console.log(discount);
}
